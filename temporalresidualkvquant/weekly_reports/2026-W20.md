# 2026-W20 周报

## 本周目标

- 从随机 head-wise baseline 转向 `importance-based top-k head-wise quant`。
- 验证 `Self-Forcing` 场景下不同 KV cache 量化方案的视频质量。
- 解决 head importance analysis 的显存问题，为全量 360 heads 重要性分析做准备。

## 本周完成

- 完成六条 `Self-Forcing` 实验线的视频生成与 VBench 评估。
- 跑通两条 packed-naive R-HWQ-4h 新实验线：
  - `packed-naive-int8 + packed-naive-int4`
  - `packed-naive-int4 + packed-naive-int2`
- 将 head importance analysis 拆成两阶段：
  - Phase 1：只做 ablation inference 并保存 latent
  - Phase 2：独立加载 DMD score model 计算 loss
- A100 80GB 上完成两阶段 smoke test，确认 OOM 问题已解决。
- 修复 Phase 2 resume 逻辑，避免部分 chunk 完成时误跳过整个 head。

## 关键实验记录

| 实验 | 配置 | Final Score | 相对 BF16 | 结论 |
|------|------|-------------|-----------|------|
| BF16 baseline | 无量化 | 0.6486 | - | 质量上界 |
| QVG INT2 baseline | PRQ INT2 all heads | 0.6469 | -0.26% | 全 INT2 PRQ 退化很小 |
| R-HWQ-4h PRQ | 4 high heads int4, others int2 | 0.6416 | -1.07% | random head-wise PRQ 可作为基线 |
| R-HWQ-4h naive | 4 high heads naive-int4, others naive-int2 | 0.5954 | -8.19% | fake/blockwise naive 退化明显 |
| R-HWQ-4h packed | high=packed-naive-int8, low=packed-naive-int4 | 0.6479 | -0.10% | 轻量 min-max 方案几乎无损 |
| R-HWQ-4h packed | high=packed-naive-int4, low=packed-naive-int2 | 0.6279 | -3.19% | 真压缩且无需 k-means，但低比特有质量损失 |

## 主要发现

- `packed-naive-int8 + packed-naive-int4` 在当前 VBench 指标上几乎无损，值得作为后续轻量化主线之一。
- `packed-naive-int4 + packed-naive-int2` 质量介于 naive 和 PRQ 之间，说明真压缩路径可用，但低比特 scalar min-max 仍需要更好的 head 选择策略。
- PRQ INT2 baseline 表现很强，说明后续方法需要明确强调成本、复杂度、校准方式或部署友好性上的优势。
- 原始单进程 head importance analysis 不适合 A100 80GB，因为 Self-Forcing KV cache 和 DMD scoring model 同时驻留会 OOM。
- 两阶段拆分后，head importance analysis 可扩展到全量 360 heads。

## 遇到的问题

- 当前 `importance-based top-k policy` 已接入代码，但尚未用全量真实 importance 文件跑视频实验。
- 现在的 head importance 只基于 focused-forcing head ablation 的 DMD loss，需要验证它是否和 identity consistency、scene consistency、motion continuity 等视频质量维度相关。
- 当前 policy granularity 仍是第一版：per-layer fixed top-k，K/V 共用同一组 heads，所有 prompts/chunks 共享 policy。

## 希望老师指导

- 是否认可把 `importance-based top-k head-wise quant` 作为论文方法主线？
- head importance metric 应优先使用 DMD loss、attention output perturbation、denoising prediction error，还是视频质量维度相关的 proxy？
- 后续对比重点应该放在：
  - 与 PRQ INT2 的质量/复杂度对比
  - 与 random HWQ 的 head selection 对比
  - 还是 packed-naive 这种无需 k-means 的部署友好路径？
- top-k policy 是否需要从 per-layer fixed 扩展到 per-chunk、sink/history/tail、K/V 分开或 prompt-adaptive？

## 下周计划

- 跑全量 360 heads 的两阶段 head importance analysis。
- 生成真实 `assets/head_importance/top4_dmd_loss.json` policy。
- 使用真实 importance policy 跑 packed-naive top-k HWQ 视频：
  `HEAD_IMPORTANCE_PATH=assets/head_importance/top4_dmd_loss.json bash scripts/self_forcing/run_packed_naive_topk_hwq.sh`
- 比较 random HWQ 与 importance top-k HWQ 的 VBench 质量差异。
- 整理实验矩阵，明确下一轮需要补充的 ablation。

## 相关路径

- 代码库：`temporalresidualkvquant/`
- 周报目录：`temporalresidualkvquant/weekly_reports/`
- VBench 汇总：`temporalresidualkvquant/results/selfforcing/vbench_eval/comparison_summary.json`
- 六条实验结果：`temporalresidualkvquant/results/selfforcing/`
- head importance 文档：`temporalresidualkvquant/docs/head_importance_topk.md`
- 两阶段脚本：`temporalresidualkvquant/scripts/self_forcing/run_head_importance_analysis.sh`
- top-k HWQ 脚本：`temporalresidualkvquant/scripts/self_forcing/run_packed_naive_topk_hwq.sh`
