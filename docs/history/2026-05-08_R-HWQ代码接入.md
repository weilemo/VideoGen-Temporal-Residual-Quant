# R-HWQ 代码接入

日期：`2026-05-08`

## 本次目标

- 在 `Self-Forcing + QVG` 代码线上接入第一版 `R-HWQ`：
  - `Random Head-Wise Quantization`

## 已完成

- 在 `experiments/Self-Forcing/inference.py` 中新增 head-wise 相关 CLI / config 参数：
  - `headwise_mode`
  - `headwise_seed`
  - `num_high_precision_heads`
  - `high_precision_quant_type`
  - `low_precision_quant_type`
- 在 `experiments/Self-Forcing/pipeline/causal_inference.py` 中接入：
  - `random` head-group policy 生成
  - mixed quant 分支
  - 动态 `num_heads` / `head_dim`
  - mixed quant 的误差打印
- 在 `quant_videogen/uncompress.py` 中新增：
  - `uncompress_single_cache()`
  - mixed group cache 的重建逻辑
- 在 `quant_videogen/kv_cache.py` 中改为按单 cache 解压，兼容 mixed group span
- 新增启动脚本：
  - `Quant-VideoGen/scripts/Self-Forcing/run_random_hwq.sh`

## 已验证

- 相关 Python 文件已通过 `py_compile` 静态检查。
- `run_random_hwq.sh` 已通过 `bash -n` 语法检查。
- 使用 `self-forcing` 环境对 `uncompress_single_cache()` 做了最小导入与拼接测试，确认 mixed head-group 的合并逻辑可执行。

## 当前状态

- 第一版 `R-HWQ` 代码路径已经接上。
- 还没有跑真实推理任务，因此当前还不能确认：
  - 真机量化路径是否完全打通
  - 日志与视频输出是否符合预期
  - `R-HWQ-4h` 是否优于 `INT2-all`

## 下一步

- 直接运行 `scripts/Self-Forcing/run_random_hwq.sh`
- 先拿到第一条 `R-HWQ-4h` 视频结果
- 再和 `BF16` / `INT2-all` 做第一轮定性对比
